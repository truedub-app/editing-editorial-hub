import 'package:flutter/material.dart';

/// Broadcast-operations palette: dark navy/graphite foundation with
/// role-coded accents (yellow = in-charge, green = QC2, blue = navigation,
/// orange/red = warnings and alerts).
class AppColors {
  AppColors._();

  static const background = Color(0xFF0D1726);
  static const card = Color(0xFF152238);
  static const cardElevated = Color(0xFF1D2D44);

  static const primaryBlue = Color(0xFF3B82F6);
  static const inChargeYellow = Color(0xFFF4C542);
  static const qc2Green = Color(0xFF39C66D);
  static const warningOrange = Color(0xFFF59E0B);
  static const errorRed = Color(0xFFEF4444);

  static const text = Color(0xFFF8FAFC);
  static const mutedText = Color(0xFF94A3B8);

  static const divider = Color(0xFF26364F);
}
